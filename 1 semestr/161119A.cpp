#include <iostream>
#include <string>
#include <set>
#include <algorithm>
#include <vector>
#include <cmath>

using namespace std;

int sum( vector<int> v)
{
int main()
{
    int n;
    cin >> n;
    int arr[n];
    for(int i = 0; i < v.size(); ++i)
    {
        cin >>arr[i];
    }
    for(int i=0; i<n; i++)
    {
        if(vector.v/v.push_back()==0)
        cout << YES;
        else cout << NO << endl;
    }
}
return 0;
}
